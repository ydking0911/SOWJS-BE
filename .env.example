# ===========================
# TEAM.GG 백엔드 환경 변수 예시
# 이 파일을 복사하여 .env로 저장 후 실제 값을 입력하세요.
# .env 파일은 절대 Git에 커밋하지 마세요!
# ===========================

# 서버 설정
HOST=0.0.0.0
PORT=4000
NODE_ENV=development

# Riot API 키 (https://developer.riotgames.com 에서 발급)
RIOT_API_KEY=RGAPI-xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
RIOT_DEFAULT_REGION=kr

# PostgreSQL (AWS RDS)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=teamgg
DB_USER=postgres
DB_PASSWORD=your_password_here

# Redis (AWS ElastiCache)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# AI 엔진 (Python FastAPI - 내부 VPC 통신)
AI_ENGINE_URL=http://localhost:8000
AI_ENGINE_TIMEOUT=30000

# CORS 허용 도메인 (쉼표로 구분)
ALLOWED_ORIGINS=http://localhost:3000,https://your-vercel-domain.vercel.app

# 로그 레벨 (trace, debug, info, warn, error)
LOG_LEVEL=info

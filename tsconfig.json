{
  "compilerOptions": {
    /* ===========================
       기본 설정
       =========================== */
    "target": "ES2022",                   // Node.js 20 LTS 지원 문법
    "module": "commonjs",                 // Node.js 모듈 시스템
    "lib": ["ES2022"],
    "outDir": "./dist",                   // 컴파일 결과물 출력 경로
    "rootDir": "./src",                   // 소스 루트 경로

    /* ===========================
       타입 안전성
       =========================== */
    "strict": true,                       // 엄격 모드 (모든 strict 옵션 활성화)
    "noImplicitAny": true,                // any 타입 암묵적 사용 금지
    "strictNullChecks": true,             // null/undefined 체크 강화
    "noUnusedLocals": true,               // 미사용 지역 변수 에러
    "noUnusedParameters": true,           // 미사용 파라미터 에러
    "noImplicitReturns": true,            // 모든 코드 경로에서 반환값 강제

    /* ===========================
       모듈 해석
       =========================== */
    "moduleResolution": "node",
    "esModuleInterop": true,              // CommonJS/ESM 상호 운용
    "allowSyntheticDefaultImports": true,
    "resolveJsonModule": true,            // JSON 파일 import 허용

    /* ===========================
       소스맵 (디버깅용)
       =========================== */
    "sourceMap": true,
    "declaration": true,                  // .d.ts 타입 선언 파일 생성
    "declarationMap": true,

    /* ===========================
       경로 별칭
       =========================== */
    "baseUrl": "./src",
    "paths": {
      "@controllers/*": ["controllers/*"],
      "@services/*": ["services/*"],
      "@models/*": ["models/*"],
      "@routes/*": ["routes/*"],
      "@config/*": ["config/*"],
      "@utils/*": ["utils/*"]
    }
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist", "tests"]
}
